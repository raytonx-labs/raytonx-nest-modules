import { createInjectionToken } from "@raytonx/core";

export const CONFIG_PACKAGE_NAME = "@raytonx/config";
export const CONFIG_MODULE_OPTIONS = createInjectionToken(CONFIG_PACKAGE_NAME, "module-options");

