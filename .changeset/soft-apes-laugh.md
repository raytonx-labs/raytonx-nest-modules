---
"@raytonx/nest-scheduler": minor
---

Add standardized task logging with `default` and `verbose` modes, including structured task lifecycle events and dedicated Redis lock failure/expiration logs for audit and inspection workflows.
